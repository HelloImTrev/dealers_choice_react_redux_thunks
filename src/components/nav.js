import React from "react";

const Nav = (props) => {
  return(
    <div className="nav-bar">
      <nav>
        <button>Books</button>
        <button>Author</button>
        <button>Favorites</button>
      </nav>
    </div>
  )
}

export default Nav;